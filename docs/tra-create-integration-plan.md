# Create integration completion plan

**Status: IN PROGRESS — A1–A3 and B1 implementation is merged; B2 is assessed below; final combined-staging verification remains pending.**
**Initial inspected baseline:** staging `15e217bba0c2cdcada3cb2d9aa73c6e495daf502` (2026-09-11).

## Goal and completion rule

Finish the missing parts and connect the existing systems to normal **Create → Astra → Sunburst → saved creative → revision**. Preserve the working foundations.

The work is complete only when every row in the inventory is built, connected to its intended user flow, and verified on the final combined staging version. A merged PR, a passing isolated subsystem test, or merely including data in a prompt is not enough.

For import/management/revision features, “connected” means their outputs are consumed by Create and retained downstream when relevant. CSV imports, approvals and revisions remain deliberate user actions. Source eligibility does not mean every asset, proof record, human or layout must appear in every ad.

## Initial findings (2026-09-11; A2 implementation below supersedes source-precedence findings)

- PRs #225 and #226 are merged. Normal Create uses `advancePortfolioPreparation`, which still stores one `analysis` chosen with source precedence. The older `prepareCreativeGeneration` also chooses layout/TRA-image/video as alternatives. Both paths require aligned fixes.
- Video Intelligence already has durable preparation/transcription/observation/finalization jobs, a transcript-linked library, and cached selection. A video upload to normal Create does not automatically consume that pipeline.
- A simultaneous TRA image can also suppress the fallback video frames in `hydrateGenerationSources`. Inspect all supplied roles independently.
- Reference category metadata and cached blueprints exist; reusable semantic angle summaries are still synthesized from category/selection rationale. Complete the missing reusable metadata rather than treating this earlier green row as finished.
- Proof Library records, API/UI and review CSV import exist. The current planner arguments do not retrieve/select Proof records.
- Human selection supports one curated human ID or a direct source. Request/provenance/UI and the standalone concept selector enforce three-frame bounds; the provider also down-selects frames.
- Company/customer-insight themes reach Astra through the runtime company profile. This is not evidence that the separate `knowledge/customer-insights.md` file is automatically loaded.
- The render brief includes headline, primary text and description. Prompt-only, TRA-image and video adapters append them again. Every adapter must honor the new image-text contract.
- The user reports 12 outputs from the latest test but poor copy/source use. This establishes reported delivery, not a full quality, cost, or end-to-end pass.

## Ownership and existing plans

This plan is the single umbrella for this integration completion work. Keep implementation acceptance and evidence here; use small linked PRs. Update concise canonical docs only for durable decisions/current status.

- [PR #222](https://github.com/celleree/tra-ai-marketing/pull/222) / `docs/human-reference-pool.md` owns the detailed human-pool and Proof requirements. Reuse and reconcile it; do not build a second competing implementation.
- [Issue #224](https://github.com/celleree/tra-ai-marketing/issues/224) owns planning speed/resumability. A/B are present; targeted repair C and optional auditor evaluation D remain separate. Fix source/copy quality first, then reassess C; D remains optional. Do not duplicate or silently close [Issue #224](https://github.com/celleree/tra-ai-marketing/issues/224).
- Coordinate with any active planning-system cleanup before editing `portfolio-preparation`, `prepare-generation`, planner schemas or persisted jobs. Do not combine unrelated cleanup into these PRs.
- Cleanup ownership remains **unconfirmed**. No identified cleanup PR is not proof that no cleanup exists. Recheck published changes before each checkpoint; stop for a concrete conflict, not merely an unanswered ownership question.
- `docs/image-workflow.md` remains the architecture authority except for the explicitly newer decisions below. The attached historical roadmap's Claude/Sol/Image-2 chain must not override current Astra/Sunburst behavior.
- Recheck live staging and active branches before each PR; this SHA is an audit baseline, not a frozen future base.

## Current decisions to record

This user request activates automatic Video Intelligence and Proof use from normal Create and replaces the old “separate Video Intelligence step required / 1–3 selected frames” direction. It also replaces the rendering assumption that all Meta copy fields are image text.

Preserve Astra Medium as strategist, the semantic audit, current Sunburst routing, exact supplied logo, brand/guardrails, durable identities, and explicit retry for uncertain paid work. Keep external Layout Reference pixels analysis-only; use their validated blueprint. No fictional/external-human fallback, fixed human quota, new model-review chain, autonomous ad publishing or Production promotion is included.

“Full video” means analyze the video's available duration and retain its transcript/scenes; retrieve compact relevant context for planning. It does not mean attach the raw video or every frame to every image request. “Full selected pool” means every eligible chosen asset remains available to selection; internal context/provider limits must not silently delete choices.

## Phases and small PR boundaries

Each listed checkpoint is a proposed smallest coherent PR, not permission to bundle a whole phase. Prefer ≤200 substantive lines; normal ceiling ≤400, split-or-justify >10 substantive files. Keep compatibility and valid intermediate states; a connected contract change may justify a larger coherent slice under existing policy.

| Phase | Outcome | Depends on | Starting route |
|---|---|---|---|
| A | One complete source inventory; video, TRA image and layout coexist | Current baseline / cleanup coordination | Astra Medium for initial A1 assessment; reassess for shared-contract implementation; Sol Medium for bounded wiring |
| B | Normal Create reuses/runs full Video Intelligence and gives it to Astra | A | Astra High for persisted-job integration; Sol Medium for adapters |
| C | Full approved human pool and exact per-ad selections | A, B | Sol High; Astra High for source/provenance review |
| D | Relevant Reviews/Case Studies reach Astra with exact evidence | A, B; review shared contracts with C | Sol High |
| E | Short natural copy; image text separated from Meta fields | A; D before final proof-render acceptance | Sol Medium; Terra Medium for narrow UI mappings |
| F | Layout fidelity and simple generation progress | A, B, E; C for human-layout acceptance | Terra Medium |
| G | Combined regression review and real staging acceptance for every row | A–F | Astra High for integration review; human quality adjudication |

### A — Compose sources without suppressing each other

- [ ] **A1:** Reconcile the current checkpoint, [PR #222](https://github.com/celleree/tra-ai-marketing/pull/222) and actual code. Establish a small versioned planning-source packet with separate video intelligence, TRA-reference analysis, layout catalog/blueprints, human candidates and proof references. Keep source IDs/hashes/version information and distinguish creative inspiration from approved evidence. Reuse existing records rather than duplicate storage.
- [ ] **A2:** Route each supplied source independently through both current entry points. In the persisted path, save each result separately; layout completion must never mean video analysis is complete. Share narrowly scoped composition/validation where it prevents divergence; leave broader extraction to the cleanup owner.

- [ ] **A3:** Complete reusable reference planning metadata using the existing reference/layout cache: stable reference ID, angle summary, layout blueprint, visual mechanism and optional curated notes/tags. Separate cached reusable analysis from per-request selection rationale; invalidate by content hash/model/schema version. Avoid a second reference store and repeated analysis of unchanged assets.

#### A1 contract checkpoint

`PlanningSourcePacketV1` in `lib/creatives/planning-source-packet.ts` defines the shared inventory using existing domain types. A1 merged in PR #228. This is a type/documentation checkpoint, not runtime integration or final staging verification; all final inventory checks remain pending.

| Packet field | Existing authority and boundary |
|---|---|
| `requestedSources` | Existing role/media-ID/SHA-256 provenance. Every supplied role remains accounted for independently. |
| `videoIntelligence` | Existing job locator/analyzer fingerprint, job ID, result artifact hash and library identity/version; bounded timestamped transcript/observation projections, explicitly unverified as evidence and provider-ineligible. |
| `traReferenceAnalyses` | Existing `CreativeReferenceAnalysis` per TRA image ID/hash. Capture model/schema/context hash when produced; unknown historical analyzer metadata stays null. Analysis is inspiration, not approved claims. |
| `referenceCatalog` | Existing `ReferencePlanningCandidate` and versioned blueprint/cache. Retain IDs/hashes/model and independent angle/layout choices; no external pixels. Reusable semantic enrichment remains A3. |
| `humanCandidates` | Existing approved-human records, selected-frame provenance and TRA-image identities. Readiness is not permission; C revalidates approval, source binding and exact fresh pixels. No external-human candidate. |
| `proofReferences` | Existing Proof record ID/type/`updatedAt` revision token. D hydrates exact text, approved wording, attribution permission, restrictions and disclaimer before use. No duplicate evidence store. |

Readiness is `PENDING`, `READY`, `UNAVAILABLE` or `RETRY_REQUIRED`; catalogs also distinguish `NOT_REQUESTED` from `READY` with zero results. Missing requested sources cannot become empty arrays or successful preparation. A supplied layout remains unresolved until its matching ID/hash is accounted for in the catalog. `READY` means data is available, never that evidence, identity or provider use is approved. These are inventory observations; existing jobs remain authoritative for leases, advancement and retry.

Keep packet data JSON-serializable and compact: no buffers, thumbnails, hydrated eligibility objects or complete video libraries. Preserve the portfolio parser's current 2 MiB bound. Company/brand context stays in its existing planner path; the separate customer-insights file is not assumed loaded. Video observations, reference inspiration and human approval do not establish factual evidence.

Do not invent missing analyses, analyzer versions or historical source choices; keep existing completed/uncertain-work retry behavior. D must resolve Proof advertising-use approval semantics; `ACTIVE`, CSV import or record presence alone does not confer permission. C owns full-pool eligibility/selection. A1 changes no request, planner response, parser, persisted job or provider behavior.

#### A2 compatibility and bounded checkpoints

Accepted compatibility: grandfather existing portfolios under their saved composition behavior, including partially prepared jobs. Preserve completed analyses, plans/audits, quota reservations, leases, frame identities and saved slots. No automatic upgrade or inferred source provenance; composed-source behavior will require a deliberate new portfolio after activation. Explicit Retry retains the existing mode and uncertain-paid-work semantics.

A2.1 introduced immutable `sourceCompositionVersion`: `1` and an absent marker preserve legacy composition; absence stays absent through reads/updates. A2.4 creates new portfolios with supported version `2` for shared composition. Unknown versions fail explicitly. This marker is independent of job/snapshot/packet schemas; existing portfolios are never upgraded.

Accepted request boundary: A2.5 may allow explicitly selected video frames alongside layouts, retaining current frame bounds and validation. Selected frames plus TRA images stay gated until C resolves human-source selection. Ordinary uploaded video + TRA image + layout composition remains in A2 scope. Render attachments and identity permissions must not expand merely because another source was analyzed.

| Checkpoint | Coherent boundary | Estimate including focused tests |
|---|---|---:|
| A2.1 | Immutable version/read/update compatibility and these decisions; new composition disabled. | 100–200 lines |
| A2.2 | Shared incremental source analysis and a distinct representative-frame analysis projection; account for all supplied roles. Existing callers remain unchanged. | 220–320 lines |
| A2.3 | Planner and snapshot/parser support for the compact A2 projection; new producers remain disabled. | 160–240 lines |
| A2.4 | Activate composition for new portfolios and the legacy request entry together; preserve per-operation checkpoints and grandfathered jobs. | 260–380 lines |
| A2.5 | Permit selected frames + layouts at the request boundary, with focused validation/integration tests. | 60–110 lines |

A2.1 merged in #229. A2.2 introduced `advancePlanningSourceAnalysis`, now used by A2.4: initialization freezes every supplied source (including repeated roles) without provider work; each advance revalidates role/media/hash plus operation/model/schema/context identity, reuses completed results, and performs at most one provider-capable operation. Reordering inputs preserves bindings; missing or changed sources stop preparation. Layout angle/blueprint analyses use separate steps. Video results identify only the representative frames actually analyzed, not full Video Intelligence, evidence approval or human permission. State is JSON-serializable; durable portfolios checkpoint it and retain existing leases and explicit retry for uncertain work.

A2.2 merged in #230. A2.3 adds optional `sourceAnalysis` to planner inputs, preparation readers and snapshots. Validation retains every source/result binding, rejects incomplete final projections or disagreement between saved planner/snapshot arguments, and preserves legacy absence and saved arguments unchanged. Astra receives source-labelled unverified observations with explicit representative-frame limitations; these confer no evidence, identity or attachment permission. Oversized jobs still fail the existing 2 MiB limit without trimming sources. A2.3 merged in #231.

A2.4 merged in #232 and activates shared composition in both preparation paths. Every supplied source and repeated role reaches Astra as a completed source-labelled result, with all uploaded image/layout candidates retained. Portfolios save each provider-capable step, including the last source analysis, before subsequent work. Explicit retry after a failed second diversity audit reuses completed version-2 source/reference preparation while requesting a new plan; legacy reset behavior stays unchanged. Accepted legacy-request boundary: the single-request entry remains non-durable with explicit resubmission, not automatic retries or a hidden resumable job. Representative-frame analysis is separate from selected render-frame identities; no full Video Intelligence or new human eligibility is implied.

Each checkpoint needs focused checks and fresh independent review of its final HEAD when required. Reassess size and published overlap after each merge; keep valid intermediate behavior. Render attachments, human selection, saved frame identities, reserved IDs and retry behavior remain unchanged. A2.5 merged in #233 and permits explicit `videoFrameSelection` with exactly one TRA video and accompanying layout references. Selected frames plus TRA images remain gated until C; ordinary video + TRA-image + layout composition stays allowed. Existing frame ownership/hash/timestamp checks, approval requirements and 1–3 unique-frame limit remain intact. Mocked request-to-Astra/render tests preserve selected frame provenance and exclude external layout pixels from rendering. A3 and B have not begun. B owns automatic Video Intelligence, C/D own human/Proof selection, A3 owns metadata enrichment and #224 owns planning optimization. Full source integration and all final staging verification remain pending. Published work was checked against refreshed staging; unpublished cleanup ownership remains unknown.

**Merged implementation checkpoint:** A2.1–A2.5 are merged through #229–#233 on staging `5f1f84971cba6250e4ab0d7195bb3a7274681aac`. A2 implementation is complete within its accepted boundaries; this is not final combined-staging verification. All final-verification rows remain pending.

**A3 assessment on that staging:** PARTIAL. Existing media IDs, validated blueprints and controlled `reusableMechanisms` already reach the catalog. Blueprint caching already keys content hash/model/schema. Missing: a reusable context-independent angle summary and its cache identity; optional curated notes/tags are absent from reference records and catalog readers. Library `angleDescription` currently includes request-specific `selectionReason`; uploaded angle analysis is bound to request context. Keep those historical records intact, retain valid blueprint cache hits, and never relabel old selection rationale as reusable analysis. The smallest next PR is an additive reusable-angle contract/cache foundation in the existing layout-cache backend, with separate semantic schema invalidation and no producer activation; subsequent bounded wiring must preserve incremental checkpoints and explicit retry. Unpublished cleanup ownership remains unknown. A3.1 below adds the disabled foundation; producer wiring remains pending.

**A3.1 contract/cache checkpoint:** Optional `reusableAngle` carries semantic schema version 1, source SHA-256, analyzer model and a bounded context-independent angle summary. Missing, malformed or source-mismatched enrichment is omitted without altering historical `angleDescription`, blueprint or saved planning arguments. Semantic entries use separate `angle-v1` keys in the existing local/R2 layout-cache backend; semantic writes never replace blueprint objects, and semantic version changes do not invalidate blueprints. No producer or provider call is activated. Optional curated notes/tags and catalog producer wiring remain for subsequent bounded work. Next: wire reusable analysis/cache reads into reference catalog preparation while preserving per-source incremental checkpoints and explicit retry; never derive reusable summaries from selection rationale. A3 and all final staging-verification rows remain pending.

**A3.2 wiring checkpoint:** A3.1 merged in #234. Both preparation paths now enrich only uploaded catalog images and the existing shortlist (at most eight library selections). Each persisted advance resolves one missing/invalid semantic entry and checkpoints before later provider work; a cache hit makes no semantic analysis call. GET/reopen and saved plans remain read-only; uncertain provider/cache-save outcomes require explicit portfolio Retry, and legacy HTTP requests retain explicit resubmission. Astra receives validated reusable summaries separately from historical angle descriptions and campaign selection rationale. Blueprint identities and render attachments are unchanged. Existing request-context TRA/layout source observations remain separate from reusable enrichment; folding redundant layout-angle observation into the reusable producer is deferred to a bounded source-contract compatibility change. Curated notes/tags storage, editing and catalog projection remain pending. A3 and all final combined-staging verification remain pending.

**A3.3 curated metadata checkpoint:** A3.2 merged in #235. Existing reference records and library cards support optional notes (2000 characters) and up to 10 tags (40 characters each), including edits and clearing. Both preparation paths freeze current curated metadata by reference ID into the new planner catalog; Astra receives it separately from machine summaries and campaign rationale, without evidence or identity approval. Edits do not alter image hashes, analyzer identities or caches; saved plans retain their snapshots. Older request-specific layout-angle consolidation remains separate. A3 is not complete and all final combined-staging verification remains pending.

#### A3 layout-angle consolidation assessment (2026-09-13)

**Baseline:** staging `a91450c932c8fb1ef8450e743566d096a3651d7a`, after #236 merged with exact reviewed HEAD `5561e9419744e41410e24a58920a95807e8d1935` and required checks passing. Initial assessment only; the subsequently authorized implementation is recorded below. A3 and final combined-staging verification remain pending.

**Evidence and distinctions:**

- `lib/creatives/planning-source-composition.ts:31–35,63–67` schedules one request-context `LAYOUT_ANGLE` operation per uploaded layout media ID, followed by its independent blueprint step. It calls `analyzeReferenceCreative(image, request.context)` and retains only `hookOrAngle.slice(0, 2000)`. Identical bytes under different IDs repeat that contextual operation. Completed source entries return without another call.
- `lib/ai/openai.ts:94–108,214–231` shows different producer inputs: the older image analysis receives user/campaign context and asks for TRA adaptation; the reusable producer explicitly receives no campaign context. The older hook can therefore contain useful campaign-conditioned interpretation not represented by the abstract reusable summary. Other older response fields (visible text, category, avoid/preserve, etc.) are discarded in this layout path, not unique downstream requirements. Do not generalize that finding to `TRA_REFERENCE`, whose full brand/content analysis remains consumed.
- `lib/references/planning.server.ts:38–61` separately resolves reusable metadata by image hash/model/semantic schema, calling its producer only on a cache miss. Selected library-only references have no old `LAYOUT_ANGLE` call; their `angleDescription` contains category/selection rationale, not a second reusable fact source.
- `composedSourceCatalog` copies the contextual hook into catalog `angleDescription`; a primary layout also copies it into `referenceAnalysis.hookOrAngle` in both preparation paths. Astra receives those plus the same hook in `sourceAnalysis`, alongside distinct `reusableAngleSummary`, curated notes/tags and campaign selection rationale (`lib/ai/creative-planner.ts:153–170`). This is repeated prompt text, not three additional provider calls. Summaries may differ legitimately; there is no evidence that dropping the contextual interpretation is quality-equivalent. Keep the distinct meanings, and leave prompt deduplication out of the cache slice.

**Smallest safe proposal: exact-context memoization, not replacement by generic analysis.** Add a separate versioned contextual-angle object in the existing local/R2 layout-cache backend, keyed by image SHA-256 + exact `request.context` SHA-256 + actual analysis model + an independent contextual prompt/schema version. Store/validate only the currently consumed bounded hook, including valid empty strings; preserve current slicing and downstream fallback. Never seed it from historical selection rationale, reusable summaries, curated notes or saved jobs. Changing context must miss; notes/tags are not inputs to this producer and must not invalidate it. Keep the existing analyzer prompt and campaign reasoning unchanged on a miss. No promise of cold-call savings or measured real-world hit rate.

Wire the helper only at the shared composition `LAYOUT_ANGLE` operation, so both new-portfolio preparation and single-request preparation benefit without changing their result contracts. Do not change the historical version-1/absent-composition branch in `portfolio-preparation.ts`. A completed saved source result, plan or checkpoint remains authoritative, even if this new cache is absent/invalid; never invalidate or reanalyze that completed work. Existing pending version-2 operations may resolve only when already authorized by the current advancement/explicit-Retry flow, using the exact existing source/context/model binding. Keep existing binding-drift failures rather than auto-refreshing a job. No migration, new source-composition version, GET work, background fill, or library-wide enrichment is needed.

**Expected image-analysis calls per uploaded layout** (source inspection, not paid measurements; exclude selector, Astra, audit and render calls; assume one ID and valid identities):

| Cache state for a fresh preparation | Current | Proposed |
|---|---:|---:|
| Everything cold | 3: contextual + blueprint + reusable | 3: same operations |
| Blueprint/reusable warm; contextual entry missing | 1 contextual | 1 contextual |
| All entries warm; identical image/model/context | 1 contextual | 0 |
| Blueprint/reusable warm; different campaign context | 1 contextual | 1 contextual |
| Existing saved contextual step complete | 0 for that step | 0; do not consult/refresh cache |

Library-only references remain 2 calls cold / 0 warm for blueprint + reusable analysis; TRA brand/content and video analysis remain unchanged. Repeated uploaded IDs with identical bytes/context can share the new cache after the first completed write; do not add parallel single-flight or claim cross-job race deduplication. Cold/malformed contextual entries cannot damage semantic or blueprint entries. A provider success followed by uncertain cache/checkpoint persistence still requires explicit Retry; a successfully persisted cache may avoid repeating the provider on that explicit retry. Legacy HTTP failures still require explicit resubmission, with no internal retry loop.

**Exact proposed edit set (one bounded follow-up PR, target <=250 substantive lines):**

- Runtime: `lib/layouts/service.ts` (contextual payload validation/key/read/write and cache-aware existing analyzer wrapper); `lib/creatives/planning-source-composition.ts` (replace only the uncached contextual operation, with provider-start notification on misses).
- Focused tests: `tests/layouts/service.test.ts` (local/R2 identity separation, warm reuse, changed context/hash/model/schema, malformed/empty hooks, unrelated-cache preservation); `tests/creatives/planning-source-composition.test.ts` (same/different-context call counts, completed-result preservation, repeated IDs and failure propagation); `tests/references/angle-preparation.test.ts` (both real entry paths, cold/warm counts, actual outbound Astra retains a contextual sentinel distinct from reusable/curated/rationale values); `tests/creatives/source-composition-preparation.test.ts` (checkpoint/reload, cache/checkpoint-save uncertainty, explicit Retry, frozen historical version-1/absent and pending/completed version-2 behavior).
- Documentation: this checkpoint. Run those tests plus `tests/creatives/portfolio-job-storage.test.ts` and `tests/creatives/portfolio-snapshot.test.ts` for saved-argument/plan invariants; required CI and fresh final-HEAD review before merge. `prepare-generation.ts`, planner schemas/prompts, job readers, blueprint schema and rendering/human code should require no edits. Stop and reassess if implementation needs to change these contracts.

**Assessment verification:** 50 existing mocked tests passed across `planning-source-composition`, `source-composition-preparation`, `angle-preparation`, `portfolio-job-storage` and layout `service`; `git diff --check` passed. This verifies current compatibility/cache behavior, not the proposed contextual cache or final staging quality. Only this document changed.

#### A3 contextual-hook cache implementation checkpoint (2026-09-13)

Implemented the bounded proposal above in `lib/layouts/service.ts` and `lib/creatives/planning-source-composition.ts` only. Separate contextual objects use image hash, exact effective context hash, actual analyzer model and independent contextual schema version. The unchanged analyzer runs on misses; validated successful hooks (including empty strings) alone are cached. Completed saved results bypass cache access. Cache-write failures propagate to existing explicit Retry/resubmission handling without an automatic provider replay.

Focused mocked coverage exercises both real preparation paths through outbound Astra, identical-context reuse and context/hash/model/schema invalidation, malformed local/R2 metadata, blueprint/reusable-object preservation, checkpoint/reload and uncertain provider/cache-write outcomes. Three existing composition/route mock fixtures also expose the new wrapper to preserve their independent regression scope; connection tests use the real wrapper and mock HTTP only. Historical composition branches, saved arguments, plans, render attachments and human permissions remain unchanged.

Expected call counts remain those in the assessment table: cold uploaded layout = 3; identical-context fully warm = 0; changed context with blueprint/reusable caches warm = 1. No paid calls or Phase B work. CI and fresh exact-HEAD review are recorded in the implementation PR. Final combined-staging verification remains pending; do not mark all A3 complete. Prompt-text deduplication remains outside this slice, with no evidence yet that it warrants runtime changes.

**Verify:** actual portfolio entry plus legacy entry with video-only, layout-only, TRA-image-only, video+layout, and video+TRA-image+layout fixtures. Assert both video and layout facts reach the real Astra request and survive checkpoint/reload. Missing required sources produce a visible, specific state.

**Start files:** `lib/creatives/{generation-sources,portfolio-preparation,prepare-generation}.ts`, portfolio preparation state/parser/snapshot, directly relevant route tests.

#### A3 acceptance reconciliation (2026-09-13)

**Current baseline:** staging `c9cbd9ede15099101e09b76993d1979af05dfad6`. PR #237 merged only after live HEAD `58525f6b6f19c3029468ab562b835243a5e475fc` matched the fresh independent approval (no findings) and all live checks passed: CI typecheck/tests/build, reviewability and Vercel. This reconciliation supersedes the earlier A3 “pending implementation/consolidation” checkpoint wording; it does not close final verification.

| A3 acceptance | Current implementation evidence | Final staging verification |
|---|---|---|
| Stable reference ID, reusable angle, blueprint and visual mechanism | `ReferencePlanningCandidate` in `lib/references/planning.ts`; independent reusable metadata from #234/#235. `LayoutBlueprint.reusableMechanisms`, geometry/background/typography carry visual mechanisms; no second mechanism field/producer needed. | Pending |
| Existing storage, content/model/schema invalidation and unchanged-asset reuse | Separate reusable/contextual objects in existing local/R2 layout backend; validated misses only. `tests/layouts/service.test.ts` and `tests/references/angle-preparation.test.ts` cover identity, corruption, unrelated-object preservation and provider counts. | Pending |
| Both preparation paths and distinct actual Astra inputs | Real preparation/HTTP-mocked integration tests retain reusable summary, contextual hook, campaign selection and curated metadata separately. Contextual cache from #237 preserves useful request-specific reasoning; cold uploaded layout still needs contextual + blueprint + reusable, fully warm identical context needs zero analysis calls. | Pending |
| Optional notes/tags edit, clear, reload and legacy absence | #236: existing record/UI; 2000-character notes, ten 40-character tags. `tests/references/curated-metadata.test.ts` verifies PATCH/storage/reload/catalog/Astra without analysis. Existing browser evidence belongs to #236; no new browser or final staging quality claim here. | Pending |
| Saved plans/results, retry and source boundaries | Composition/cache tests plus portfolio storage/snapshot regressions retain frozen results/arguments, explicit Retry and legacy resubmission, independent blueprints and render/human permissions. No automatic enrichment on reopen. | Pending |

**Implementation coverage: COMPLETE for A3 as written. Concrete remaining A3 runtime gap: none found.** Historical composition still behaves historically by design. Repeated contextual text in the prompt is not a duplicate provider call or demonstrated defect; no speculative prompt cleanup is proposed. Existing A3/inventory checkboxes remain unchecked because the document's completion rule includes final combined-staging acceptance. Later B/C/D/E/F work is not missing A3 metadata.

### B — Use the Video Intelligence already built

- [ ] **B1:** Attach/reuse the existing video job/library by source hash and analysis version from the portfolio preparation state. A user-started Generate can drive bounded existing job steps; GET/reopen is read-only. Do not call the entire pipeline inside one long portfolio request. Preserve leases, authentication, operator budgets, stable IDs, result recovery, and explicit retry after an uncertain paid step.
- [ ] **B2:** Provide timestamped transcript excerpts, visual observations, scene coverage, known frame IDs and relevant selection context to Astra. Cache reuse must work across reload/resume and repeated use of unchanged media. Include later-video information, not only the opening frame or thumbnail. No extra generic video-analysis model call is needed when the intelligence already contains that information.
- [ ] **B3:** Bring cached intelligent selection into Create and provide source progress/error handling. Handle silent/no-speech video explicitly; use visual information without fabricating a transcript. Failed/unavailable required analysis must not silently fall back to thumbnail-only planning. Make a deliberate reduced-source choice visible if supported.

**Verify:** a fixture with a distinctive useful message only late in the video; a fresh source, cached source, changed hash, silent video, interrupted preparation, and uncertain provider outcome. Record transcript/library IDs and actual planner input. Reopening a completed analysis makes zero new analysis calls.

**Reuse:** `lib/video/intelligence-service.ts`, existing job runners/client, `frame-library`, `selection-context`, `selection-cache`, `concept-selection`; update only proven blockers in extraction.

#### B1 assessment and proposed sequence (2026-09-13; documentation only)

**Status: NOT CONNECTED to normal Create portfolios. Existing subsystem is reusable.** `portfolio-preparation.ts` currently delegates video to `advancePlanningSourceAnalysis`, which extracts representative stills and calls `analyzeApprovedTraVideoFrames`. It never starts/advances a Video Intelligence dependency. `PlanningSourcePacketV1.videoIntelligence` is a type-only shape, not a persisted runtime producer/consumer. `selection-context.ts` can load an already completed durable library for selected-frame handling; that does not implement automatic preparation from an ordinary video upload.

**Existing authorities and concrete integration constraints:**

- `intelligence-service.ts` supplies START/STATUS/ADVANCE/RETRY and current identity resolution; `intelligence-job-store.ts` owns deterministic jobs, CAS, leases, saved work and uncertain paid outcomes. START reuses an existing job; a new START runs preparation only. ADVANCE runs one claimed phase/unit, not the whole pipeline. Observation claims contain up to two representatives; preserve that existing bounded batch instead of claiming “one provider call” per portfolio advance. Preparation is non-provider work but may be substantial; never follow it with another phase in the same portfolio request.
- Identity is **media ID + source SHA-256 + analyzer fingerprint**, not hash alone. The fingerprint includes pipeline version, candidate policy, vision model and transcription model (`intelligence-preparation.ts`); job/library/manifests validate source ID as well as hash. Same stored asset/hash/settings reuses across portfolios; changed bytes/settings cannot reuse. Identical bytes uploaded under a different media ID do not currently share a job. Recommend preserving this binding for B1 rather than reassigning source provenance or adding a second index/store.
- `loadVideoIntelligenceLibrary` verifies immutable artifact key/hash/length and source/model binding; `loadVideoIntelligencePreparation` validates manifest identity. Library includes transcript, timestamps, all candidate identities, representative observations and semantic groups. Save compact references/projections, not its thumbnails or up-to-32-MiB body, in the portfolio's 2-MiB record.
- Existing service locator resolution reconstructs **current** settings and rejects fingerprint drift. Pending new-mode dependencies should fail visibly on drift, not silently restart. Completed portfolio projections/planner arguments remain authoritative on resume; do not resolve them again through current settings or refresh their caches. Reads must parse recorded identity structurally, without substituting current settings.
- Auth, deployment availability and VIDEO_PREPARATION/VIDEO_PROVIDER_WORK quotas currently live at `app/api/video/intelligence/jobs/route.ts`, not inside `executeVideoIntelligenceStep`. Calling the service internally must preserve that admission before mutations/provider work; the portfolio's CREATIVE_PLANNING reservation does not cover it. Thread a deadline from request entry, retain checkpoint reserve, and recheck the portfolio lease before child advancement. Service START currently does not forward the deadline to its preparation runner; assess this bounded non-provider phase against the request budget, not a promise of cancellable extraction.
- Portfolio Retry currently clears a planning error only; it cannot authorize a child RETRY by inference. Persist an explicit, single-use authorization tied to the saved dependency/retry state, consume it before executing RETRY, and require another deliberate Retry after a lost/uncertain attempt. Reload a child completion after parent checkpoint loss instead of rerunning it. Parent and child leases remain independent; no provider calls inside a CAS update callback.
- Stop is currently a client scheduling boundary (`portfolio-client.ts`), not a durable server cancellation flag. Preserve “stop after in-flight work settles”: checkpoint that result, schedule no next child phase, resume only on deliberate action. Do not cancel a shared child job needed by another portfolio. Busy child status must produce bounded waiting/polling, not a tight ADVANCE loop or the client's existing “progress did not advance” error. Progress must reflect saved child checkpoints even while `sourceAnalysis` remains pending.
- GET/reopen stays read-only: no START, claim, expired-lease recovery, quota reservation, RETRY or library regeneration. Mutation occurs only in authorized advancement/retry. Missing/corrupt required artifacts produce an explicit unavailable/retry state; do not hide failure with generic still analysis.

**Recommended coherent sequence; estimates include focused tests/checkpoint, subject to the 400-line/10-file policy:**

| Slice | Deliverable and valid intermediate behavior | Estimated substantive size |
|---|---|---:|
| B1.1 — persisted dependency contract, disabled | Add optional immutable `videoPreparationVersion: 1` independently of source composition; a compact dependency list binds supplied TRA_VIDEO ID/hash to locator/job and optional completed artifact/library identity. Validate known versions, duplicates, roles, binding, bounds and completed-reference preservation. New portfolio creation does **not** set the marker yet; absent marker retains exact existing behavior. | 220–300 lines, 8 files |
| B1.2 — bounded adapter, disabled | New portfolio-side helper uses existing service/store/loaders: discover/save locator first, then START or one ADVANCE/explicit RETRY, then return/checkpoint. Reuse COMPLETE without provider calls; preserve admission, deadline, child/parent lease and retry-authorization boundaries. Test real state machines with mocked provider/runner boundaries. No active caller yet. | 240–350 lines, about 5–7 files |
| B1.3 — minimum completed-intelligence handoff, disabled | Add the smallest validated source-analysis/planner projection needed to consume completed library facts distinctly and avoid the generic representative-frame call. Reuse the A1 packet vocabulary; include source/job/artifact identity and bounded timestamped transcript/observation fields without thumbnails. Preserve a legacy parser branch, snapshots and saved arguments. This is the necessary B1/B2 interface dependency; broad relevance retrieval/selection work stays B2/B3. | 250–350 lines, about 7–9 files |
| B1.4 — activate new portfolios and lifecycle | Wire marked new portfolios before source analysis; expose saved dependency progress/busy status; connect explicit Retry and client wait/stop/resume. Set marker only after adapter and outbound projection are ready together. No-video portfolios stay unchanged. Completed intelligence replaces generic representative analysis only for the new mode. Keep historical and non-durable paths unchanged as below. | 280–400 lines, about 8–10 files |

**Exact B1.1 first-slice files:** new `lib/creatives/portfolio-video-dependency.ts` (types and pure bounded validator), `lib/creatives/portfolio-preparation.ts` (optional dependency state type only), `lib/creatives/portfolio-job.ts` (optional mode type only; constructor stays disabled), `lib/creatives/portfolio-job-parser.ts`, `lib/creatives/portfolio-job-storage.ts` (immutable mode and completed dependency bindings), new `tests/creatives/portfolio-video-dependency.test.ts`, `tests/creatives/portfolio-job-storage.test.ts`, and this document. Do not edit runners, routes, planner or UI in the first slice.

**Focused checks:** B1.1 tests round-trip absent marker and old partial/completed portfolios, reject unknown versions/bad or duplicate IDs/hash/fingerprint/artifact references and oversized metadata, preserve completed dependency bindings/immutable mode, and prove reads/new portfolio creation make no video calls. Run existing portfolio job/parser/storage/snapshot regressions. B1.2 uses `tests/video/intelligence-{service,job-store,transcription-runner,observation-runner,finalization-runner}.test.ts` plus new adapter tests: cold start, warm complete, changed source/settings, busy shared child, expired leases, successful child checkpoint with lost parent save, partial observation failure, no silent RETRY, quota denial and deadline boundaries. Activation tests must traverse real portfolio advancement/storage and actual outbound Astra with mocked HTTP: video-only and mixed sources, later-video sentinel, silent video, missing library, warm reuse, zero generic representative-analysis calls in the new mode, saved dependency reload, stop after in-flight work, and explicit retry authorization. Assert no provider/storage mutations on GET/reopen and preserve attachment IDs/permissions. Required CI and fresh exact-HEAD review remain necessary per runtime PR.

**Legacy entry and compatibility decision:** recommend grandfathering every existing portfolio (including current version-2 partial jobs) by absence of the new immutable video marker. Do not migrate on GET/Resume/Retry or discard completed representative analysis. The non-durable `prepareCreativeGeneration` / `app/api/creatives/generate/route.ts` has no portfolio dependency checkpoint or durable Retry token: retain its current representative-frame composition and explicit HTTP resubmission. Do not call the durable pipeline in its request or pretend its failures have portfolio recovery. Automatic full intelligence for that entry remains explicitly outside B1; a future deliberate redirect to a durable portfolio, or cache-only completed-library handoff with a visible miss, requires its own contract.

**Material decisions before activation:** accept media-ID-bound hash/version reuse (cross-ID deduplication would require a separate provenance-safe design); accept opt-in only for newly created portfolios and the explicit legacy limitation; accept the minimum B2 projection as an activation prerequisite rather than paying for full intelligence then ignoring it. Recommend all three. Keep the existing up-to-two-observation child unit and stop-after-in-flight semantics; no subsystem rebuild, new provider, raw-video attachment, evidence approval or human selection expansion. B1 alone will not complete B2/B3 or final staging verification.

#### B1.1 inactive dependency contract checkpoint (2026-09-13)

The three B1 decisions above are accepted: reuse remains media-ID/hash/analyzer-version bound; existing portfolios and the non-durable entry remain grandfathered; a usable completed-library projection must reach Astra before activation replaces representative-frame analysis.

B1.1 adds optional immutable `videoPreparationVersion: 1` and compact `videoDependencies` in preparation. Each entry reuses `VideoIntelligenceJobIdentity` (including its saved analyzer fingerprint) and the existing deterministic job ID, with optional completed artifact/library references. The identity contains the fields needed to derive the service locator later; no second stored locator or video store. Validation is pure and checks recorded settings, not current environment. Unknown versions, malformed/foreign/duplicate identities, mismatched known source hashes and extra payload fields are rejected. Entries are bounded to 4 KiB each within the unchanged 2-MiB portfolio limit. Artifact references are structurally validated here; B1.2 must use the existing loader to verify actual stored artifact bytes.

Activation remains disabled: constructors do not set the marker or dependencies; no service runner, route, planner, provider or render integration is added. Saved bindings cannot be replaced/removed; completion can be appended once. Because references currently live in preparation, leaving that phase cannot discard them: B1.3 must provide the preserving handoff before activation. Existing unmarked jobs, saved results, leases and explicit Retry retain their behavior.

| Slice | Existing B2/B3 ownership mapping |
|---|---|
| B1.1 contract | Supplies durable reference vocabulary only; no B2 content projection or B3 selection/UI implementation. |
| B1.2 bounded adapter | Owns dependency lifecycle/reuse/uncertain-work admission. B3 reuses its readiness/failure states instead of a second job runner. |
| B1.3 minimum handoff | Implements the minimum B2 timestamped transcript/observation projection and actual Astra connection required for B1 activation. Record that B2 subset once; remaining relevance/scene coverage tests stay B2. |
| B1.4 activation | Connects normal Create and its basic saved dependency progress/Retry. This covers the corresponding B3 source-progress portion; B3 still owns cached intelligent selection and deliberate reduced-source behavior. |

Do not duplicate these handoffs in later B2/B3 PRs or mark those requirements complete merely because a library reference is stored. Stop here before B1.2. Focused verification and fresh exact-HEAD review/required CI are recorded in the implementation PR. A3 implementation acceptance remains complete as reconciled above; every final combined-staging verification row remains pending. No paid generation.

#### B1.2 bounded adapter checkpoint (2026-09-13)

**Baseline: B1.1**, merged in PR #238 after live reviewed HEAD `7413da0abf822d9e8734cf2522091b76ff2ee1c2` and passing required checks were verified. **B1.2 merged in PR #239; reviewed HEAD `69609c8` was verified green, producing staging `95a72db56d45dcb5e25be21184d3f8c4b4e75aa6`.** It adds inactive `stepPortfolioVideoDependency`: discovery checkpoints the existing identity/job reference before a later START, and each subsequent call performs at most one existing bounded job unit. Existing services own child leases, checkpointed progress and finalization. Valid completed artifacts are loaded and bound to the compact dependency; saved completion does not consult current analyzer settings or rehydrate/reanalyze the video. Pending source/settings drift fails explicitly.

The adapter checks deployment availability, requires an owning lease/checkpoint interface and operator video quota admission before child mutation, retains the supplied deadline/checkpoint reserve, and returns busy/failed/retry-required status without looping. STATUS performs no saves, quotas, claims or provider work. A Retry call requires the owner to durably consume a single-use authorization bound to the observed child retry state. An optional child ETag precondition, forwarded through the existing service to its CAS retry claim, rejects races after authorization consumption; callers without this option retain existing behavior. These two small subsystem edits are necessary to prevent a shared job's later uncertainty from being retried under an earlier authorization.

Owner callbacks are deliberately required integration ports: B1.4 must connect authenticated portfolio execution, lease-checked saves, durable authorization consumption and existing parent uncertain-work error handling. Callback/provider/save failures propagate without internal replay; a successful child checkpoint can be recovered on a later explicitly authorized parent attempt. No automatic scheduler, production caller, new dependency store or parent retry behavior is introduced here. Tests use real service/store/quota/finalization/library and portfolio checkpoint persistence, with hydration/provider-capable runner boundaries mocked.

Focused tests cover discovery/save/reload, cold bounded progression, warm complete reuse and missing artifacts, identity drift, busy/expired leases, quota/deadline/ownership rejection, explicit Retry and changed child revision, provider uncertainty and lost parent saves. Production preparation/legacy-route regression tests assert zero Video Intelligence service calls. Existing representative-frame analysis, render attachments and all unmarked portfolios remain unchanged.

B1.3 still owns completed-library content projection and actual Astra delivery; B1.4 owns activation and parent lifecycle wiring. B2/B3 mapping above remains authoritative. Activation is disabled, no paid generation occurred, and all final combined-staging verification rows remain pending. CI and fresh independent final-HEAD review are recorded in the implementation PR. Stop before B1.3.

#### B1.3 minimum completed-intelligence handoff checkpoint (2026-09-13)

**B1.3 merged in PR #240, producing staging `046ca38a3564eb050b262a847b1b6e8f368b2376`.** The handoff replaces each matching representative-video source-analysis entry with a validated `VIDEO_INTELLIGENCE` projection only after the existing dependency and full library are complete. It preserves full dependency/job/artifact/library identity, source hash, transcript timing and representative frame identity while excluding thumbnails. Transcript excerpts and observations are deterministically bounded across the full timeline (24 and 16 respectively); saved observations retain their original representative ordinal, and exact sample indexes plus chronological timestamps are validated on reload. Explicit total counts and `COMPLETE` versus `UNIFORM_TIMELINE_V1` coverage prevent the bounded packet from implying full content coverage. Silent video is represented explicitly.

The same validated projection reaches the actual Astra request beside unchanged layout/source entries and remains distinct from advertising evidence, claims, human approval, identity permission and render attachments. Planner arguments and snapshots preserve it through checkpoint, reload, audit/repair and explicit Retry without consulting current analyzer settings or library availability. At this checkpoint production activation remained disabled for B1.4 to route marked new portfolios through the completed projection before advancing source analysis, thereby avoiding the generic representative-frame analysis call. Broad relevance retrieval, complete scene coverage and intelligent frame selection remained B2/B3 work. No paid generation or final combined-staging verification occurred.

#### B1.4 automatic durable video preparation checkpoint (2026-09-13)

**B1.4 merged in PR #241 after reviewed HEAD `195a7fe` and required checks passed, producing staging `44e35f7cd2bfb55a7705ecaf5b87ad99b3b222c6`.** New durable portfolios containing a `TRA_VIDEO` source now receive immutable `videoPreparationVersion: 1`. Normal bounded portfolio execution discovers and saves each existing Video Intelligence dependency, advances at most one child unit per request, saves child progress/completion under the parent lease, loads each completed library, and applies the B1.3 projection before remaining source analysis and the actual Astra planning request. A completed exact-identity library is reused without representative-frame analysis. Existing unmarked portfolios, the non-durable generate route, human sources and render attachments retain their prior paths.

GET and public progress reads remain read-only. Reopen does not start provider work; Resume continues saved bounded work, and Pause stops after the in-flight request settles. Public progress exposes only total/completed video counts, phase and busy state. Exact child retry state plus ETag stays inside the strictly validated saved preparation contract. Explicit parent Retry copies that exact observed state into a single-use authorization, which is consumed durably under the parent lease before the child retry claim. If the child revision has changed, execution refreshes status without provider work and requires another explicit Retry. The route threads one request-entry deadline through child execution, preserving existing admission, quota, lease and uncertainty safeguards.

Focused mocked verification covers cold preparation, a second new portfolio's completed-library cache reuse, two distinct videos plus a layout in the same actual Astra payload, saved interruption/reload, Pause/Resume, shared-child busy progress, failure and exact-state Retry recovery. The complete-path integration uses the real portfolio execution, Video Intelligence adapter/service/store/finalization, B1.3 projection, source preparation and planner request; only storage hydration, provider HTTP and adjacent layout/reference boundaries are mocked. It verifies zero representative-frame analyzer calls and excludes thumbnails from Astra. PR #241's required CI and fresh independent final-HEAD review passed before merge. No paid calls or final combined-staging verification occurred.

This completes the minimum B2 handoff already mapped to B1.3 and the B3 saved source-progress/Retry portion mapped to B1.4. B2 still owns broad relevance and complete scene coverage; B3 still owns intelligent selection and deliberate reduced-source behavior. Later work must use these contracts rather than duplicate the child runner, projection or progress lifecycle.

#### B2 completed-intelligence context assessment (2026-09-13; documentation only)

**Assessed baseline:** staging `44e35f7cd2bfb55a7705ecaf5b87ad99b3b222c6`, including PR #241 at reviewed HEAD `195a7fe` with required checks green and merged.

**Overall classification: PARTIAL.** The completed library now reaches the actual Astra request and survives portfolio checkpoints, but the current bounded projection is not relevance-aware and does not preserve enough temporal or semantic context to claim B2 complete. This assessment does not reopen B1 lifecycle work or include B3's per-concept intelligent frame selection.

| B2 requirement | Classification | Minimal current evidence |
|---|---|---|
| Completed library identity and content reach the projection | COMPLETE | lib/creatives/video-intelligence-planning.ts:99-148 verifies dependency/library identity and creates VIDEO_INTELLIGENCE; lib/creatives/planning-source-parser.ts:26-74 revalidates it. |
| Projection reaches the actual Astra request | COMPLETE | lib/ai/creative-planner.ts:135-170 parses and serializes the exact sourceAnalysis; tests/creatives/video-intelligence-planning.test.ts:78-102 asserts equality at the mocked HTTP boundary. |
| Reload/resume and unchanged-media reuse retain the same context without another analysis call | COMPLETE for the implemented B1 path | tests/creatives/video-intelligence-planning.test.ts:142-169 covers persisted planner/snapshot reload; tests/creatives/portfolio-video-integration.test.ts:118-133 traverses two portfolios, reuses completed child jobs and reaches Astra without representative-frame analysis. Final deployed verification remains G. |
| Timestamped transcript content | PARTIAL | video-intelligence-planning.ts:115-132 retains timestamps but chooses at most 24 individual segments by uniform segment ordinal. It does not preserve contiguous passage windows. With 30 segments, ordinal 1 is selected while adjacent ordinal 2 can be omitted, so an outcome statement can survive without its nearby qualification or case-study context. |
| Visual observations and source-bound frame identity | COMPLETE for the bounded selected observations | video-intelligence-planning.ts:117-135 retains timestamp, frame ID/hash, original representative ordinal, observation and overlapping transcript for at most 16 observations. Thumbnails remain excluded. |
| Early/middle/late and scene/topic coverage | PARTIAL | Sampling is uniform by representative ordinal, not elapsed-time bucket, scene type or topic (video-intelligence-planning.ts:29-30,117-135). A deterministic 100-second, 30-representative probe at 0 seconds, 50 seconds, then 85–98.5 seconds selected ordinals 0, 2, 4, 6, 8, 10, 12, 14, 15, 17, 19, 21, 23, 25, 27 and 29, omitting the available middle-third scene at ordinal 1. The projection also drops the library's semanticGroups and all unsampled observations. Existing tests prove a final late sentinel survives, not balanced early/middle/late or rare-scene coverage (tests/creatives/video-intelligence-planning.test.ts:78-90). Upstream candidate/library coverage is a separate extraction concern and is not blamed on this projection result. |
| Known frame IDs | COMPLETE for projected facts | Every projected observation retains its stable source-bound frame ID and SHA-256 (video-intelligence-planning.ts:79-95,117-135). B2 does not require every library frame to be a planning candidate; which observations belong in the bounded projection is the separate coverage/relevance gap. |
| Campaign-relevant transcript/observation context | NOT STARTED | Projection receives no campaign context and uses no relevance ranking. The library's scene/topic groups are not projected. lib/video/concept-selection.ts has a separate cached model selection over frame metadata; connecting that per creative remains B3 and must not be used to call B2 complete. |
| Observed claims remain separate from approved advertising evidence | COMPLETE as a boundary | Library/projection values remain providerEligible: false and UNVERIFIED_MODEL_OBSERVATION; creative-planner.ts:36-39,161 tells Astra that transcript/visible claims are source content and grant no claim, evidence, identity or attachment permission. Matching a video passage to an approved Proof record remains D4. |

**Smallest coherent next PR — B2.1 context-preserving projection v2:** preserve the outer VIDEO_INTELLIGENCE analyzer binding at schemaVersion: 1; that binding is already frozen and validated by source composition. Add an independently dispatched inner projectionVersion: 2. The parser must retain an exact legacy branch for the current unversioned B1.3 context, treated as projection v1, and new projections must use distinct coverage values rather than changing the meaning of UNIFORM_TIMELINE_V1.

The v2 transcript replaces isolated excerpts with bounded windows: each window carries firstSegmentIndex, lastSegmentIndex, startMs, endMs, selectionReasons (EARLY, MIDDLE, LATE, or OBSERVATION_CONTEXT) and the original contiguous segments. Keep at most 24 total segments and the existing 64-KiB combined context limit. A selected seed reserves its immediate predecessor and successor before remaining capacity is filled; merge overlaps, never truncate source text, and stop filling before the byte cap. If the mandatory temporal seeds plus their context cannot fit, fail closed rather than silently claim their coverage. This protects one-segment adjacency only; longer case-study narratives and separated qualifications remain part of the later relevance/context work. The v2 observation coverage uses ELAPSED_TIME_BUCKETS_V2 and records EARLY/MIDDLE/LATE bucket availableCount and includedCount; reserve one observation from each nonempty elapsed-time third, then fill the existing 16-observation limit deterministically. Each projected observation adds its bucket as a selectionReason. This slice preserves local passage context and honest temporal coverage; it does not claim semantic relevance or approved evidence.

**Exact proposed B2.1 files and size:** lib/creatives/planning-source-packet.ts, lib/creatives/video-intelligence-planning.ts, lib/ai/creative-planner.ts, tests/creatives/video-intelligence-planning.test.ts, tests/creatives/portfolio-video-integration.test.ts, tests/ai/creative-planner.test.ts, and this document; estimated 180–260 substantive lines across seven files. planning-source-parser.ts and planning-source-composition.ts should remain unchanged unless type checking proves a dispatch dependency: both already delegate inner validation to parseVideoPlanningContext, and changing the outer analyzer binding would create unnecessary saved-state incompatibility.

**Focused B2.1 verification:** add fixtures proving an available middle observation at an otherwise omitted ordinal is retained; early/middle/late bucket counts are truthful; a selected transcript statement retains its adjacent qualifier; silent video remains explicit; legacy unversioned v1 projections parse and round-trip byte-equivalent through saved planner arguments/snapshots; malformed v2 windows/buckets, duplicates and the 64-KiB limit fail closed; and actual Astra JSON contains v2 context beside layout data with no thumbnails or approved-evidence implication. Run:

    ./node_modules/.bin/vitest run --configLoader runner tests/creatives/video-intelligence-planning.test.ts tests/creatives/portfolio-video-integration.test.ts tests/ai/creative-planner.test.ts

Then run the existing portfolio parser/storage/snapshot focused regressions, typecheck and normal CI. This is MEDIUM integration-boundary work and needs fresh independent review of the final exact HEAD.

**B2.1 implementation checkpoint (2026-09-13):** the inner v2 projection is implemented while the outer `VIDEO_INTELLIGENCE` schemaVersion 1 binding and exact unversioned v1 parser branch remain intact. Transcript retrieval now emits explicit bounded contiguous windows with source segment indexes, bounds and selection reasons. Mandatory elapsed-time seeds reserve their immediate neighbors before optional fill; overlapping windows merge, the 24-unique-segment cap is enforced, and optional neighborhoods are rolled back whole when the 64-KiB serialized-context limit would be exceeded. Observation-local transcript copies must be byte-identical members of an `OBSERVATION_CONTEXT` window, so they cannot extend transcript coverage outside that cap; their serialized duplication still counts toward the 64-KiB budget.

Observations now reserve one frame from every nonempty elapsed-time third, disclose per-bucket available/included counts and fill deterministically up to 16 only while the combined context fits. Existing completed v1 or v2 projections are validated and preserved byte-equivalently when the projection helper runs again, including a mixed saved/new source set. Planner guidance makes omitted observation coverage and the lack of semantic/campaign relevance explicit while retaining the provider-ineligible, evidence, identity and pixel-attachment boundaries. Focused verification passed 79 mocked tests across the three B2.1 files plus portfolio parser/storage/snapshot composition regressions, and typecheck passed. No paid generation occurred. Required CI and fresh independent exact-HEAD review are recorded in the implementation PR; final combined-staging verification remains pending.

**Remaining B2 after B2.1:** design and prove a bounded relevance/semantic-scene projection from the full completed library without a new generic video-analysis call. Simple keyword matching is insufficient evidence of semantic relevance. Keep this planning-context retrieval separate from B3's cached per-concept attachment selection and from D4's approved Proof linkage. Decide its compact scene/relevance index and byte/token budget from adversarial fixtures before implementation; until that work passes actual outbound-Astra tests, campaign relevance, rare scene/topic coverage and longer case-study context remain incomplete.

#### B2.2 campaign-conditioned retrieval assessment (2026-09-13; documentation only)

**Assessed baseline:** staging `c2bfc835aca0dab24f355f4e12fb6d80736396ff`, including B2.1 PR #242 at reviewed HEAD `1bb3f684f57085ddb6f371d33079783d108b9d42`; its required checks passed before merge.

**Overall classification remains PARTIAL.** B2.1 gives Astra honest temporal coverage and short contiguous context, but `projectCompletedVideoIntelligence` receives no campaign input. The same completed library therefore produces the same projection for every campaign. Astra can reason differently because `creativeContext` differs; that is not evidence that the bounded source retrieval selected the campaign-relevant parts of the full saved library.

A real-code mocked-HTTP probe used one completed 60-segment/24-observation library with an eight-segment customer story and a separate IRS-document passage. Both contrasting requests reached the actual `requestCreativeBatch` HTTP boundary. The customer-story and IRS-process requests had different `creativeContext` strings but byte-identical `sourceAnalysis`. The current v2 projection contained exactly 24 transcript segments and 16 observations and serialized to 13,102 bytes, within the 64-KiB limit. It retained story indexes 29–31 while omitting setup/process indexes 26–28 and a later qualification at index 32. This proves the current gap without implying that every adjacent segment belongs to one semantic story. A scratch lexical experiment selected different seven-segment neighborhoods for the two briefs, showing a bounded deterministic improvement is feasible when terms overlap; it did not prove synonym, paraphrase, story-boundary or semantic relevance.

**Smallest bounded B2.2 behavior:** create a per-portfolio v3 projection over the already loaded completed library. Pass exactly the validated original `job.request.context`, not the later enriched planning prompt. This is the user creative direction only, not the complete effective Astra/company brief. Preserve the reusable library/job/analyzer identity and cache. Persist an independently versioned selector binding containing the exact SHA-256 of that user direction plus the canonical effective query-term subset used by the selector. Normalize deterministically with NFKC, lowercase alphanumeric tokens, a fixed versioned stopword set, stable first-occurrence deduplication and at most the first 64 eligible unique terms. Record total, included and omitted eligible-term counts so query truncation is explicit. The saved portfolio parser must recompute and match that binding against `job.request.context`. Existing unversioned v1 and v2 contexts remain byte-equivalent and are never enriched or upgraded on reload, Resume, audit repair or explicit Retry.

Use only saved transcript segments and saved observations. First reserve B2.1's mandatory early/middle/late transcript neighborhoods. Then rank overlapping contiguous core passages of up to eight segments by distinct query-term overlap, total overlap and stable source order. Add each core's immediate predecessor and successor when they exist, and admit that core-plus-guards atomically as at most ten unique segments. The chosen bound covers the assessed eight-segment fixture plus local boundary context while leaving at least five unique-segment slots after the worst-case nine mandatory temporal segments inside the 24-segment limit. Omit the whole candidate when the segment or 64-KiB budget cannot hold it, then apply optional uniform fill. For observations, reserve the early/middle/late representatives first, then positive lexical matches ranked from saved summary, composition, visible text, scene type and topics, then deterministic fill within 16. Record `CAMPAIGN_LEXICAL_MATCH` selection reasons, explicit bounded/truncated state and exact available/included/omitted positive-candidate counts. Empty, stopword-only or off-topic briefs retain the B2.1 temporal fallback with zero positive matches.

Call this method deterministic lexical campaign matching, not semantic retrieval. A bounded core plus guards preserves nearby context; it does not identify a case-study boundary or prove that every qualification was found. Synonyms, paraphrases, concepts absent from the user direction, stories longer than the admitted candidate and qualifications outside it can still be omitted. The 16-observation limit also prevents an unconditional promise that every semantic group will have a detailed observation. These limitations remain visible in existing temporal coverage plus the new relevance and query-omission fields. No generic analysis, embedding/model call, second global cache, automatic reanalysis or provider call is added. All transcript and observation content remains `UNVERIFIED_MODEL_OBSERVATION`, `providerEligible: false`, and separate from claim approval, human identity, pixel attachment and Proof authority.

**Smallest coherent PR and exact files:** keep selector contract, parser, saved-request binding, activation and actual Astra delivery together because an inactive selector would not satisfy B2.2 and activation without the saved-state guard would weaken authority. Change only `lib/creatives/planning-source-packet.ts`, `lib/creatives/video-intelligence-planning.ts`, `lib/creatives/portfolio-execution.ts`, `lib/creatives/portfolio-job-parser.ts`, `lib/ai/creative-planner.ts`, `tests/creatives/video-intelligence-planning.test.ts`, `tests/creatives/portfolio-video-integration.test.ts`, and this document. Estimate 400–550 substantive lines across eight files, justified as one atomic versioned persisted-context boundary. Stop and reassess before adding `planning-source-parser.ts` or storage/schema files; the existing parser already delegates the inner video context. Do not combine B3 attachment selection or D4 Proof authorization.

**Focused acceptance:**

- Contrasting customer-story and IRS-process briefs over the same completed library select different positive transcript passages and observations when exact normalized terms support that distinction; the saved binding matches the exact original request context.
- An in-budget eight-segment case-study fixture with relevance terms, outcome and qualifications distributed across its segments is retained as one complete atomic core plus its available immediate guards. A separate longer or more distant-qualification fixture proves the bounded candidate limit and truthful bounded/truncated metadata rather than claiming complete narrative recovery.
- Empty, stopword-only, off-topic, synonym-only and paraphrase-only briefs exercise the declared lexical limits while retaining truthful B2.1 temporal coverage.
- v1/v2 saved contexts parse and round-trip byte-equivalently and are never upgraded. A saved v3 reload, Resume, audit repair and explicit Retry retain the exact selected context and selector binding without loading the library or selecting again.
- Malformed selector metadata, noncanonical terms/order, request-context binding mismatch, false coverage counts, non-atomic passage admission, duplicate indexes/identities and contexts over 64 KiB fail closed before provider work.
- The actual mocked Astra HTTP body contains the v3 selection beside unchanged layout data, with at most 24 unique transcript segments, at most 16 observations, at most 64 KiB per video context, no thumbnails, and unchanged evidence/identity/attachment qualifiers. The projection adds zero provider calls.

Run the focused projection test first, then the integration test. Rerun the current three-file planning set and the portfolio parser/storage/snapshot regressions, followed by typecheck and normal CI. This is MEDIUM persisted-state and planner-integration work and needs fresh independent review of the final exact HEAD. Final deployed and human quality verification remains G.

**B2.2 assessment verification:** 42 existing mocked tests passed across `video-intelligence-planning.test.ts`, `portfolio-video-integration.test.ts` and `creative-planner.test.ts`. One additional untracked `/tmp` mocked-HTTP probe passed while invoking the real current projection and planner request boundary; `/tmp/b22-current-probe.json` recorded the exact selections, counts and byte size reported above. `git diff --check` passed. Only this document changed in the worktree; no runtime/test files, provider calls, paid generation or final staging verification were performed.

**B2.2 implementation checkpoint (2026-09-13):** new completed-library projections use inner `projectionVersion: 3` and bind deterministic lexical retrieval to the exact original portfolio `request.context`. The saved selector records its SHA-256, the first 64 eligible unique NFKC/lowercase Unicode letter-or-number terms in stable order, and total/included/omitted counts under a fixed English stopword list. Portfolio parsing recomputes this binding for preparation, checkpoint planner arguments and snapshots. Existing v1/v2 contexts remain exact and are preserved without reloading or normalizing their library when the projection helper sees a completed saved context.

V3 first reserves B2.1 temporal transcript neighborhoods and observations. It then ranks one maximal contiguous core of up to eight segments at every source start by distinct term overlap, total overlap and source order. A selected core and its immediate available guards are admitted or rolled back together within 24 unique segments and 64 KiB; positive observations are ranked from saved summary, composition, visible text, scene type and topics before deterministic fill within 16. Saved core/guard intervals, lexical selection reasons and exact available/included/omitted positive-candidate counts make the bounded result auditable. The actual Astra request receives this context beside unchanged layout data; no thumbnail, generic analysis, embedding, reanalysis or provider call was added.

**B2 remains PARTIAL after B2.2.** Exact lexical overlap improves campaign-conditioned retrieval but does not establish semantic relevance, synonym or paraphrase recall, case-study boundaries, narratives beyond the admitted core, distant qualifications, or complete semantic-group observation coverage. Query truncation and omitted positive candidates are explicit; empty, stopword-only, off-topic, synonym-only and paraphrase-only directions retain temporal fallback with zero lexical matches. All content remains unverified and provider-ineligible, separate from Proof, claim approval, identity permission and pixel attachment. This change supplies no evidence that B3 cached per-creative frame selection, attachment routing or deliberate reduced-source behavior is implemented; B3 remains unchanged. Focused mocked projection/planner/portfolio integration tests passed 45 checks, parser/storage/snapshot/composition regressions passed 43 checks, saved reload/Retry checks passed, and typecheck passed. At this pre-commit checkpoint, required CI and fresh exact-HEAD review are pending; PR records carry final gate results. Final combined-staging verification remains pending. No paid generation occurred.

**Historical A3/B1 assessment verification:** 96 mocked tests passed across reference planning/curated metadata/angle preparation, layout service, portfolio storage/snapshot, Video Intelligence service/job-store/client and selection-context. `git diff --check` passed. Only the plan document changed on `docs/a3-b1-assessment`; no B1 runtime edits, paid generation or final staging verification were performed. Those tests established reusable foundations, not passing coverage for the then-proposed B1 integration.

**B2 assessment verification:** this assessment reran 5 focused mocked tests in `video-intelligence-planning.test.ts` and `portfolio-video-integration.test.ts`, covering current projection parsing/persistence, actual outbound Astra delivery, completed-job reuse and retry-state handling. Deterministic local probes demonstrated the omitted-middle-scene and adjacent-qualifier cases above. `git diff --check` passed. Only this document changed on `docs/b2-assessment`; no runtime/test edits, provider calls, paid generation or final staging verification were performed. The passing current tests establish the implemented B1 handoff and expose the missing B2 assertions; they do not make the partial B2 rows complete.


### C — Full human pool, selected separately for each creative

- [ ] **C1:** Add a versioned unified eligible source catalog covering selected video frames, existing approved-human records and approved `TRA_REFERENCE` images. Preserve access to all user-selected candidates using metadata/paging if needed. Avoid an arbitrary catalog slice hiding relevant selected sources.
- [ ] **C2:** Remove the three-frame selection restriction across UI, request validation, saved provenance, selector response/cache and any downstream extraction assumptions. Display the usable library, not merely a three-result shortlist. Validate nonempty/unique IDs, source ownership, hashes, approval, timestamp and decodable fresh PNGs. Keep real technical/provider budgets internal.
- [ ] **C3:** Astra selects validated human source IDs/subsets per creative. The renderer receives exactly that subset; remove silent first/middle/last re-selection. Support multiple eligible source types in a portfolio without blending different identities or treating Layout Reference people as eligible. Revalidate at render and revision time, including revoked/missing/stale sources; retain exact actual attachment provenance.

**Verify:** >3 selected frames through the UI and request, a useful candidate beyond the old first three, different source choices across creatives, both video and TRA-image candidates, and no-human/graphic briefs. Check attached IDs/hashes against planned IDs/hashes. Inspect actual humans visually; correct attachment alone cannot prove identity fidelity.

### D — Ground evidence using the existing Proof Library

- [ ] **D1:** Add relevant, bounded retrieval over eligible ACTIVE Reviews and Case Studies using the existing store and metadata. Pass exact review text, approved claim wording, restrictions, disclaimer, attribution permission and stable record identity to Astra. Check the existing authorization meaning of ACTIVE; CSV import or active status alone must not manufacture permission for advertising use. If required evidence/use approval is not recorded, add the smallest explicit approval field/workflow to the existing Proof schema/UI and leave unconfirmed records ineligible. Missing relevant proof stays unavailable; no new evidence database or mandatory embedding/model layer.
- [ ] **D2:** Add per-creative evidence selection and validation. Hydrate known records, reject unknown/inactive/stale/unsupported selections, and preserve exact quotes or explicitly source-bound excerpts without changing their meaning. Attribution needs the record's permission. Verified facts do not authorize universal promises.
- [ ] **D3:** Persist used proof ID/version or content hash, selected text/claim and restrictions separately from human/layout provenance. Revalidate before paid rendering and revisions; retain historical snapshots without claiming revoked proof is currently eligible. Wire imported reviews and current case-study UI records into this same retrieval path.
- [ ] **D4:** Connect useful video case-study passages to existing approved Proof records. Surface unmatched passages as timestamped candidates for TRA's review through the existing evidence workflow; do not automatically approve model-extracted facts. Verify distilled customer-insight content reaches Astra once, separately from exact proof.

**Verify:** an imported multiline exact review, allowed vs disallowed attribution, a case study with restrictions/disclaimer, no relevant proof, inactive/edited evidence, and a source passage with an unsupported outcome. Confirm exact used proof in actual planner output, render brief, saved creative, reload and revision. If a chosen review cannot fit the image naturally, choose an approved shorter excerpt or keep it in Meta copy; never rewrite a quotation to fit.

### E — Fix copy and image text together

- [ ] **E1:** Introduce backward-compatible `adCopy` (Meta primary text/headline/description) and explicit `imageCopy` (image headline, optional short support, optional proof/attribution, optional CTA, applicable disclosure) in plan/persistence/revision contracts. Image text is intentional; no automatic promotion of Meta primary text or description onto the image. Keep legacy records readable without inventing historical choices.
- [ ] **E2:** Use plain consumer language: one clear idea, short natural sentences, no redundant explanation. Primary text adds context; headline is a hook. Reject normalized identical headline/primary text and use existing planning/audit/human review for meaning-level repetition. No extra universal model call just for copy. As normal design guidance, aim for a 3–8-word image headline and at most one short support line; proof-led layouts and required disclosures are explicit exceptions. Validate mobile legibility, not word count alone.
- [ ] **E3:** Update render brief plus prompt-only, TRA-image, video-frame and revision adapters so only `imageCopy` can become image text. Remove appended Meta fields. Preserve selected proof, required disclaimers, source rules and exact logo handling. Map Meta fields correctly in cards/details/export without displaying headline twice.

**Verify:** capture every adapter's actual outbound prompt and attachments; sentinel Meta-only text is absent from the image brief; required disclosures/selected proof are present. Check create → save → reload → edit/regenerate/variation → 4:5/9:16. Inspect real outputs for awkward copy, repetition and text overload.

### F — Respect chosen layouts and show useful progress

- [ ] **F1:** Preserve the selected blueprint's meaningful hierarchy, region count, image position, whitespace and restrained text density. For the reported example, explicitly test headline + approved man on the left + approved review below, with relevant proof. Keep angle/layout independent and user-priority references optional overall; test chosen-layout fidelity separately from the percentage of ads choosing it. Do not add raw external layout pixels or extra badges/cards/sections.
- [ ] **F2:** Replace the machinery-heavy progress panel with **“Planning 12 creatives…”**, **“0 / 12”**, a visual progress bar, and **“Stop generation”** (count reflects actual requested N). Show useful video/planning/generating/stopped states. Completed count comes from saved slots; no fabricated planning percentage. Stop prevents new work after the in-flight operation settles, preserves results, and makes that state clear. Reopen loads automatically but starts no paid work; Resume and uncertain-work Retry remain explicit.

**Verify:** desktop/mobile/keyboard, accessible live progress and bar values, zero/partial/completed counts, Stop during video/planning/render, reopen/resume and failure/retry. Keep optional technical details out of the normal flow.

### G — Prove the complete flow and close the table

- [ ] **G1:** Run focused integration tests at each boundary and existing CI for full tests/typecheck/build; obtain independent review of the final current PR HEAD where required. After integration, review the combined staging change, not just individual PRs. Any changed HEAD invalidates a required earlier exact-HEAD review.
- [ ] **G2:** Validate actual staging R2 upload, image display, source hydration, download, signed-link refresh and allowed-origin/CORS behavior. Keep deployed configuration and app release identity in the evidence.
- [ ] **G3:** Run a real 12-ad portfolio from normal Create with a video containing useful later content, simple layout reference, available approved TRA human images, and relevant approved Review/Case Study records. First use controlled fixtures/mocks to eliminate avoidable failures. Record source coverage and actual requests/records, then inspect every delivered ad.
- [ ] **G4:** Exercise refresh/Stop/resume, one edited creative and 1:1/4:5/9:16 variants, missing/revoked sources, no-human and graphic briefs. Run 36 only after the 12-ad quality/cost gate passes and the existing bounded paid-validation authorization covers it; otherwise record the exact remaining budget/asset/access need.
- [ ] **G5:** Revisit every inventory row below. Attach evidence and only then set final staging verification to ✅. Reopen any row invalidated by a later change. Close this workstream only with no partial/failed/unverified in-scope rows.

**Quality gates from the active checkpoint:** 12/12 technically valid and saved; ≥10/12 acceptable first-pass; all 12 planned hypotheses strategically distinct; all 12 acceptable/distinct after at most one targeted correction per rejected slot. Scale: 36/36 saved, ≥30/36 acceptable and distinct first-pass, all 36 acceptable/distinct within the same correction allowance. Preserve zero unauthorized identity/claims/logo violations. Do not lower these thresholds to make the table green.

**Record:** deployed SHA/URL, portfolio ID, input media/library/proof IDs and versions, selected-vs-attached source IDs, actual prompts/brief versions, cold vs cached video analysis calls/time/cost, plan/audit/repair/render calls, tokens/cache hits where available, total timing, and `12 generated / technically successful / acceptable first-pass / strategically unique`. Do not manufacture unavailable metrics.

**Cost guard:** reuse cached video/layout/selection/Proof context, avoid full transcripts/images in every render, do not re-run completed provider work, and preserve the existing successful plan+audit path. First-time transcription/observations are real work and may add cost compared with the previously bypassed flow; report that separately. Do not promise zero added total cost or silently downgrade quality. Keep [Issue #224](https://github.com/celleree/tra-ai-marketing/issues/224)'s planning-cost guard and compare cost per accepted distinct ad.

**Final per-row evidence format:** `row ID | final staging SHA | focused/integration check | portfolio/creative ID or browser evidence | result | remaining blocker`. No placeholders or inherited green checks count as final verification.

## Complete feature inventory

Built/connected columns are the initial implementation inventory, using the earlier table plus targeted current-source inspection; they are not fresh release certification. ✅ = present, ⚠️ = partial, ❌ = missing or unavailable through normal Create. “Final verified” starts ⏳ for every row. Connected for optional flows means consumed when relevant/supplied. The original 41 rows are retained; eight reported requirements are added.

| ID | Part | Built? | Connected? | Work or retest | Phase | Final verified |
|---|---|---:|---:|---|---|---:|
| 01 | Astra creative strategist/planner | ✅ | ✅ | Preserve Astra Medium; inspect actual Create planner input and accepted plan. | A, G | ⏳ |
| 02 | Astra portfolio diversity planning | ✅ | ✅ | Keep global hypotheses and source choices through all new integrations. | A, G | ⏳ |
| 03 | Layout Blueprint extraction | ✅ | ✅ | Reuse cached blueprint; retain selected geometry on actual output. | A, F, G | ⏳ |
| 04 | Layout Reference → Astra | ✅ | ✅ | Reach planner alongside video/TRA analysis; verify chosen layout and original choices. | A, F, G | ⏳ |
| 05 | Reference Library | ✅ | ✅ | Retest catalog discovery, selected IDs, and user-reference priority. | A, G | ⏳ |
| 06 | Reference planning metadata | ⚠️ | ⚠️ | Reuse existing category/blueprint cache; finish reusable angle/mechanism metadata and cache invalidation. | A, G | ⏳ |
| 07 | TRA Reference ad analysis | ✅ | ⚠️ | Run when supplied even alongside a Layout Reference or video. | A, G | ⏳ |
| 08 | Video Intelligence | ✅ | ❌ | Automatically reuse/start its existing durable job from normal Create. | B, G | ⏳ |
| 09 | Video transcription | ✅ | ❌ | Process full video audio; persist timestamped transcript; deliver relevant content to Astra. | B, G | ⏳ |
| 10 | Scene/frame extraction | ✅ | ⚠️ | Use full-duration intelligence candidates rather than just the fallback representative set. | B, G | ⏳ |
| 11 | Visual video observations | ✅ | ⚠️ | Deliver source-bound observations with transcript context, including later scenes. | B, G | ⏳ |
| 12 | Transcript-linked video frame library | ✅ | ❌ | Load the completed library into Create; preserve source identity and temporal links. | B, G | ⏳ |
| 13 | Intelligent video-frame selection | ✅ | ❌ | Use existing selection intelligence from Create; remove graphics-first bias for human selection. | B, C, G | ⏳ |
| 14 | Selected video frames → generation | ✅ | ⚠️ | Use the exact per-ad chosen frames and record actual attachments. | C, G | ⏳ |
| 15 | Approved video-human frames | ✅ | ✅ | Preserve fresh verified PNG extraction and source/approval checks. | C, G | ⏳ |
| 16 | Approved Human Library | ✅ | ✅ | Reuse existing records, approval/revocation, and metadata; widen discovery as required. | C, G | ⏳ |
| 17 | Full human-source pool for Astra | ⚠️ | ❌ | Make every eligible selected source discoverable; internally page/index large pools. | C, G | ⏳ |
| 18 | Unlimited user-selected video frames | ❌ | ❌ | Remove arbitrary 1–3 UI/request/provenance caps; keep real validation and internal budgets. | C, G | ⏳ |
| 19 | Astra chooses human per creative | ⚠️ | ⚠️ | Choose a validated subset per ad across the unified pool; do not add a human quota. | C, G | ⏳ |
| 20 | TRA_REFERENCE humans as approved human sources | ⚠️ | ⚠️ | Include eligible TRA image sources alongside video sources; preserve identity permissions. | C, G | ⏳ |
| 21 | Exact human provenance | ✅ | ✅ | Extend existing provenance to all new per-ad subsets and revisions; check actual pixels too. | C, G | ⏳ |
| 22 | Proof Library | ✅ | ❌ | Retrieve eligible records during normal planning using the existing store. | D, G | ⏳ |
| 23 | Reviews storage/API/UI | ✅ | ❌ | Keep existing management; records become available to normal Create evidence retrieval. | D, G | ⏳ |
| 24 | Review CSV import | ✅ | ❌ | Imported exact reviews become retrievable; import stays an explicit user action. | D, G | ⏳ |
| 25 | Case-study/proof records | ✅ | ❌ | Use existing verifiedFacts/approvedClaimWording/restrictions/disclaimer records. | D, G | ⏳ |
| 26 | Proof retrieval for Astra | ❌ | ❌ | Add bounded relevant retrieval and validated stable proof IDs. | D, G | ⏳ |
| 27 | Exact proof provenance in generated ads | ❌ | ❌ | Save used record/version/hash plus exact approved wording/quote, attribution and restrictions. | D, G | ⏳ |
| 28 | Company/brand context | ✅ | ✅ | Retest runtime company profile, customer-insight themes and brand rules in planner input. | A, D, G | ⏳ |
| 29 | TRA guardrails | ✅ | ✅ | Preserve hard source rules; prevent unapproved quotes/outcomes and retain applicable disclaimers. | D, E, G | ⏳ |
| 30 | Distilled Sunburst render brief | ✅ | ✅ | Version it to contain only explicit on-image copy and that ad's selected execution inputs. | E, G | ⏳ |
| 31 | Sunburst image rendering | ✅ | ✅ | Retest prompt-only, TRA-image, video-human and revision adapters with the new contracts. | C, E, G | ⏳ |
| 32 | Layout-reference pixels blocked from final generation | ✅ | ✅ | Preserve current analysis-only policy; transfer approved blueprint geometry. | F, G | ⏳ |
| 33 | Portfolio semantic audit | ✅ | ✅ | Retain global audit; same-category distinct concepts remain eligible. | G | ⏳ |
| 34 | Bounded portfolio repair | ✅ | ✅ | Preserve current bound; [Issue #224](https://github.com/celleree/tra-ai-marketing/issues/224) PR C still owns targeted replacement optimization. | G | ⏳ |
| 35 | Resumable generation jobs | ✅ | ✅ | Persist added preparation dependencies without replaying completed/uncertain paid work. | B, G | ⏳ |
| 36 | 2–36 ad portfolios | ✅ | ✅ | Retest 12, then 36 only after the quality and cost gates pass. | G | ⏳ |
| 37 | Persisted progress/results | ✅ | ✅ | Verify save/reload/returned images and provenance on the final deployment. | B, F, G | ⏳ |
| 38 | Pause/reopen/resume infrastructure | ✅ | ✅ | Retest stop admission, read-only reopen, resume, explicit retry and stable IDs. | B, F, G | ⏳ |
| 39 | Creative revisions | ✅ | ✅ | Carry new text/evidence/human contracts through edits, regeneration, variation and formats. | C, D, E, G | ⏳ |
| 40 | R2 media storage | ✅ | ✅ | Retest upload/storage/read through the real staging bucket and durable source records. | G | ⏳ |
| 41 | Direct browser R2 media delivery | ✅ | ✅ | Verify browser upload/display/download, signed-link refresh and live origin/CORS behavior. | G | ⏳ |
| 42 | Video + Layout Reference used together | ❌ | ❌ | Both current generation paths must retain independent video and layout analyses. | A, B, G | ⏳ |
| 43 | Plain, natural consumer copy | ⚠️ | ⚠️ | Enforce clear short copy without corporate filler; judge the rendered ads at phone size. | E, G | ⏳ |
| 44 | Headline and Meta primary text have different roles | ❌ | ❌ | Reject duplicate normalized fields; assess repetitive meaning during existing review. | E, G | ⏳ |
| 45 | Separate on-image text from Meta ad fields | ❌ | ❌ | Explicit imageCopy vs adCopy; stop all adapter-level primary-text/description leakage. | E, G | ⏳ |
| 46 | Simple layouts and restrained text density | ⚠️ | ⚠️ | Respect selected region count/hierarchy; use only planned optional support/proof/CTA. | E, F, G | ⏳ |
| 47 | User-facing progress bar and Stop generation | ⚠️ | ❌ | Show Planning N creatives… / saved count N / progress bar / Stop generation. | F, G | ⏳ |
| 48 | Exact TRA logo and placement safe zones | ✅ | ✅ | Preserve deterministic supplied-logo placement and test 1:1, 4:5 and 9:16. | G | ⏳ |
| 49 | Video case-study insights → approved evidence use | ⚠️ | ❌ | Surface timestamped candidate facts; link to approved Proof records before claim use. | B, D, G | ⏳ |

## Source anchors for this baseline

- [Active Create preparation](https://github.com/celleree/tra-ai-marketing/blob/15e217bba0c2cdcada3cb2d9aa73c6e495daf502/lib/creatives/portfolio-preparation.ts), [legacy preparation](https://github.com/celleree/tra-ai-marketing/blob/15e217bba0c2cdcada3cb2d9aa73c6e495daf502/lib/creatives/prepare-generation.ts), [source hydration](https://github.com/celleree/tra-ai-marketing/blob/15e217bba0c2cdcada3cb2d9aa73c6e495daf502/lib/creatives/generation-sources.ts).
- [Reference records](https://github.com/celleree/tra-ai-marketing/blob/15e217bba0c2cdcada3cb2d9aa73c6e495daf502/lib/references/types.ts), [reference planning catalog](https://github.com/celleree/tra-ai-marketing/blob/15e217bba0c2cdcada3cb2d9aa73c6e495daf502/lib/references/planning.server.ts).
- [Existing Video Intelligence service](https://github.com/celleree/tra-ai-marketing/blob/15e217bba0c2cdcada3cb2d9aa73c6e495daf502/lib/video/intelligence-service.ts), [concept selection](https://github.com/celleree/tra-ai-marketing/blob/15e217bba0c2cdcada3cb2d9aa73c6e495daf502/lib/video/concept-selection.ts), [frame selection contracts](https://github.com/celleree/tra-ai-marketing/blob/15e217bba0c2cdcada3cb2d9aa73c6e495daf502/lib/video/generation-selection-contract.ts).
- [Human candidate loading](https://github.com/celleree/tra-ai-marketing/blob/15e217bba0c2cdcada3cb2d9aa73c6e495daf502/lib/video/approved-human-planning.ts), [video image adapter](https://github.com/celleree/tra-ai-marketing/blob/15e217bba0c2cdcada3cb2d9aa73c6e495daf502/lib/ai/video-frame-generation.ts), [render routing](https://github.com/celleree/tra-ai-marketing/blob/15e217bba0c2cdcada3cb2d9aa73c6e495daf502/lib/creatives/render-planned.ts).
- [Proof record types](https://github.com/celleree/tra-ai-marketing/blob/15e217bba0c2cdcada3cb2d9aa73c6e495daf502/lib/proof/types.ts), [existing Proof storage](https://github.com/celleree/tra-ai-marketing/blob/15e217bba0c2cdcada3cb2d9aa73c6e495daf502/lib/proof/storage.ts), [company context](https://github.com/celleree/tra-ai-marketing/blob/15e217bba0c2cdcada3cb2d9aa73c6e495daf502/lib/company/creative-context.ts).
- [Planner](https://github.com/celleree/tra-ai-marketing/blob/15e217bba0c2cdcada3cb2d9aa73c6e495daf502/lib/ai/creative-planner.ts), [render brief](https://github.com/celleree/tra-ai-marketing/blob/15e217bba0c2cdcada3cb2d9aa73c6e495daf502/lib/creatives/render-brief.ts), [TRA image adapter](https://github.com/celleree/tra-ai-marketing/blob/15e217bba0c2cdcada3cb2d9aa73c6e495daf502/lib/ai/openai.ts), [prompt-only adapter](https://github.com/celleree/tra-ai-marketing/blob/15e217bba0c2cdcada3cb2d9aa73c6e495daf502/lib/ai/prompt-only-generation.ts), [revision adapter](https://github.com/celleree/tra-ai-marketing/blob/15e217bba0c2cdcada3cb2d9aa73c6e495daf502/lib/ai/creative-revision-image.ts).
- [Progress panel](https://github.com/celleree/tra-ai-marketing/blob/15e217bba0c2cdcada3cb2d9aa73c6e495daf502/components/creative-generator/portfolio-progress-panel.tsx), [active image workflow](https://github.com/celleree/tra-ai-marketing/blob/15e217bba0c2cdcada3cb2d9aa73c6e495daf502/docs/image-workflow.md), [quality gates](https://github.com/celleree/tra-ai-marketing/blob/15e217bba0c2cdcada3cb2d9aa73c6e495daf502/docs/image-gen-improvement.md).

## First bounded Codex task

Routing for A1:

```text
WHERE: Codex Sidebar in VS Code
SESSION: NEW — TRA Create Integration A1
MODEL: GPT-6 Astra
REASONING: Medium
PARALLEL: NO — shared preparation, source and persistence contracts must be settled first
```

**TASK:** A1 only: settle the shared source packet and reconcile current product/checkpoint direction with this integration plan.

**CONTEXT:** Normal Create follows the persisted portfolio path; video+layout currently suppresses video analysis. #225/#226 are merged. Video Intelligence and Proof storage already exist.

**SOURCE OF TRUTH:** `AGENTS.md`, this plan A1 and its invariant decisions, current `docs/image-workflow.md`, [PR #222](https://github.com/celleree/tra-ai-marketing/pull/222), and the directly relevant source/preparation types.

**SCOPE:** Inspect current staging and overlapping cleanup work; specify the smallest compatible source composition contract and the first coherent implementation slice. Update only concise durable docs/types needed for A1. Do not add provider calls, rebuild subsystems, or begin the later phases.

**INSTRUCTIONS:** Follow existing PR-size and review rules. Use a dedicated feature branch from current staging. Use existing session authorization for reversible work; escalate only genuine unresolved product decisions. Keep main unchanged.

**RETURN:** PR/checkpoint, exact relevant files, agreed source packet, compatibility/verification evidence, overlap/dependencies, remaining gaps, and the next bounded A2 task. Later PRs should use the cheapest suitable model from the phase table; reassess rather than inheriting the A1 route.
